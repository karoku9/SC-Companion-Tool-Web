function init(){cacheElements(),load(),initShips(),applySettings(),bindStaticEvents(),bindAutocomplete(),initMapControls(),renderMissions(),updateShip(),renderMap(),renderContext(null),timeline(state.calculated&&state.route.length?state.route:buildRoute(),!state.calculated),state.calculated&&(el.status.className=state.active?"draft-pill is-active":"draft-pill is-ready",el.status.innerHTML=state.active?"<i></i>Active":"<i></i>Ready",el.startRoute.disabled=!1,el.calc.querySelector("span").textContent="Recalculate",state.active&&(el.startRoute.innerHTML='<svg><use href="#icon-arrow"/></svg>Open active route')),renderActiveRoute(),renderFleet(),renderIntel()}init();